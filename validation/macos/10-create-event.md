# Create Event

> Platform: **macos** · Mockup: _missing — still required_ · Status: **blocked**

## Source
- `apps/ios-macos/Sources/LikemindedMac/MacScreens.swift (createEvent)`
- `apps/ios-macos/Sources/LikemindedMac/MacAppState.swift (createMeeting)`
- `apps/ios-macos/Sources/LikemindedMac/LikemindedAPIClient.swift (createMeeting)`
- `services/api/src/server.js (POST /v1/meetings)`
- `apps/ios-macos/Assets.xcassets/EventJazzListening.imageset`

## Entry points
- Communities tab create-event route
- `--mac-screen createEvent`

## Backend dependencies
- `POST /v1/meetings`
- `data/validation-db/mvp-store.json`

## Visual parity vs mockup
- Result: `blocked`
- Layout: runtime uses bottom nav and now fills the maximized content area with panels
- Imagery: Add cover shows a contextual hand-drawn jazz-listening doodle asset
- Blocker: no Create Event mockup exists

## Control ledger

| Control | Type | Expected | Result | Evidence |
|---|---|---|---|---|
| Meetup | button | Selects Meetup | pass | Computer Use shows `Value: Selected` by default. |
| Listening Session | button | Selects Listening Session | pass | Computer Use click changed selected state to Listening Session. |
| Jam Session | button | Selects Jam Session | pass | Reachable button with selected/not-selected value; same shared `eventTypeButton` path. |
| Event name, Date, Time, Location, Details | text fields | Editable fields drive preview/create payload | blocked | Exposed as settable and seeded values drive preview/POST. Current Computer Use tool has no type action; macOS key events did not mutate focused SwiftUI field, so typed editing/blank-title validation is unproven. |
| Add cover | button | Adds contextual cover to preview | pass | Computer Use click changed label to `Cover added`, added `EventJazzListening` image, and displayed doodle cover. |
| Add tags | button | Adds visible preview tags | pass | Computer Use click changed label to `Tags added` and preview displayed `Meetup` + `Community hosted`. |
| Create event | button | Persists meeting and stays on correct screen | pass | Computer Use showed creation status without navigation drift; validation DB contains persisted meeting title/date/location/details/group status. |
| Bottom nav | navigation | Visible and not covering controls | pass | Default, small, and maximized windows keep primary controls above bottom nav. |
| Traffic lights | window controls | Native close/minimize/full-screen visible | pass | Computer Use exposes close, minimize, and full-screen/zoom controls. |

## Window Sizes

| Size | Result | Evidence |
|---|---|---|
| 1200x760 | pass | All primary controls, traffic lights, and bottom nav visible. |
| 1120x752 | pass | Form, preview, create button, and bottom nav remain reachable. |
| 4480x1368 maximized | pass | Panels fill the available content area; nav and traffic lights remain visible. |

## Notes
Screen is no longer a placeholder. Remaining blockers are missing Create Event mockup and typed text-field validation in the current Computer Use session.

---
_Generated by `validation/_generate.js`. Update `result`/`evidence` after runtime testing._
