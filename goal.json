{
  "schema_version": 1,
  "status": "completed",
  "purpose": "Per-session execution goal. Copy this file to goal.json at session start, set the current goal, and keep graders/validation/agent routing current when files or commands change.",
  "goal": "Track — macOS Unimplemented Controls: wire Settings nav + Soulmate prefs, Chat/Messages header actions, Community detail rows, and Profile voice/onboarding steps 2–3; close 24 pending validation/macos/*.json stub rows. Infra-blocked Apple sign-in and LiveKit join remain Phase 9/10.",
  "ultimate_goal_source": "GOAL.md",
  "progress_source": "PROGRESS.md",
  "workflow_refs": [
    "docs/references/macos-screen-audit.md",
    "docs/workflows/validation.md",
    "docs/product-direction.md"
  ],
  "design_references": {
    "owner": "DESIGN.md",
    "purpose": "Single source of truth for Like-minded visual language, copy tone, interaction principles, and iOS/macOS design rules.",
    "mockups": {
      "ios": "mockups/ios/",
      "macos": "mockups/macos/"
    },
    "usage": [
      "Before iOS native UI work, consult DESIGN.md and the relevant screen-range images in mockups/ios/.",
      "Before macOS native UI work, consult DESIGN.md and the relevant screen-range images in mockups/macos/.",
      "Skip mockups for backend-only, validation-only, or non-visual changes."
    ]
  },
  "validation_inventory": {
    "local_simulator": {
      "script": "script/verify_simulator_local.sh",
      "command": "npm run verify:simulator-local",
      "workflow_ref": "docs/workflows/validation.md",
      "purpose": "Build, install, and launch the simulator app; prove fresh auth gate, empty-dev-db Profile onboarding, and DEBUG signed-in backend placement tabs.",
      "reuse_when": [
        "Native app changes.",
        "Auth or session flow changes.",
        "apps/ios-macos/project.yml changes.",
        "Local API auth or MVP placement routes change.",
        "script/verify_simulator_local.sh changes."
      ]
    },
    "external_preflight": {
      "script": "script/verify_external_preflight.js",
      "command": "npm run verify:external-preflight",
      "workflow_ref": "docs/workflows/validation.md",
      "purpose": "Verify real Render, Neon, Apple, LiveKit, TestFlight, and manual proof recorded in release/testflight-evidence.json.",
      "template": "release/testflight-evidence.template.json",
      "evidence_file": "release/testflight-evidence.json"
    }
  },
  "validation_state": {
    "local_simulator": {
      "status": "<pending|passed|stale>",
      "last_verified_at": "",
      "command": "npm run verify:simulator-local",
      "artifacts": [],
      "proves": [],
      "does_not_prove": [
        "Real Apple sign-in.",
        "Real spoken audio creating persisted profile and placement.",
        "Signed TestFlight build behavior."
      ],
      "do_not_repeat_unless_inventory_inputs_changed": true
    },
    "external_preflight": {
      "status": "<pending|waiting_for_user_external_setup|passed>",
      "command": "npm run verify:external-preflight",
      "missing_evidence_file": "release/testflight-evidence.json",
      "next_agent_action": "Treat as a later release gate after repo-owned hardening is complete; ask the user to fill release/testflight-evidence.json only when external setup and device proof are actually ready."
    }
  },
  "release_gate": {
    "external_preflight": "Run npm run verify:external-preflight only after Render, Neon, Apple Developer, App Store Connect/TestFlight, and manual device proof are ready."
  },
  "agent_guardrails": [
    "Lazy context: goal:next + narrowest lane command first; stop when answered. Do not preload GOAL.md, full PROGRESS, mockups, or source trees for status questions.",
    "Lazy authoring: edit control owners (validation JSON, PROGRESS track, workflow command table) — do not add parallel docs or status tables.",
    "Proactive prune: delete stale triggers, sibling ledgers, duplicate routing, historical prose posing as current truth in the same pass as fixes.",
    "If goal.json status is completed, select the next coherent unchecked surface from PROGRESS.md before expanding retrieval.",
    "Avoid tiny one-checkbox goals unless they are the only blocker; group adjacent small items under the same owner surface.",
    "Check validation_state / ledger:open before rerunning completed proof; re-test stale_pass rows only.",
    "Reuse validation_inventory scripts and ledger scripts before creating new ones.",
    "Before claiming phase/track/goal complete, run npm run verify:ledger-progress. Actionable fail/pending/stale_pass/stale-blocked controls need unchecked PROGRESS owners; JSON is sole control-status owner.",
    "When a control is fail or stub, add or keep an unchecked PROGRESS item in the same session."
  ],
  "deterministic_graders": [
    {
      "name": "node_syntax",
      "command": "npm run check",
      "cost": "zero_token",
      "proves": "JavaScript entrypoints and grader scripts parse."
    },
    {
      "name": "mvp_backend_contract",
      "command": "npm run smoke:mvp",
      "cost": "zero_token",
      "proves": "Auth-gated discovery, persisted profile/placement, placement action, feedback, meetings RSVP/scheduling, LiveKit token generation, notifications, community members, soulmate preferences, and cross-user isolation work locally."
    },
    {
      "name": "release_static_config",
      "command": "npm run verify:release-config",
      "cost": "zero_token",
      "proves": "TestFlight-critical bundle id, entitlements, API base URL, LiveKit env vars, Render env, and privacy-policy files are configured."
    },
    {
      "name": "goal_contract",
      "command": "npm run verify:goal",
      "cost": "zero_token",
      "proves": "goal.json and goal.template.json contain the current grader, validation, rubric, and agent routing contract."
    },
    {
      "name": "ledger_progress_ownership",
      "command": "npm run verify:ledger-progress",
      "cost": "zero_token",
      "proves": "Every actionable fail/pending/stale-blocked control in validation/{ios,macos}/*.json has an unchecked PROGRESS.md owner track item."
    },
    {
      "name": "repo_docs_lint",
      "command": "workflow --docs-dir /Users/gurusharan/Documents/remote-claude/active/apps/Like-minded-app/docs lint",
      "cost": "zero_token",
      "proves": "Repo docs follow the local workflow documentation contract."
    },
    {
      "name": "validation_data_lifecycle",
      "command": "npm run reset:validation-data",
      "cost": "zero_token",
      "proves": "Validation API can attach a separate local database and seed deterministic backend data for profile, circles, communities, meetings, soulmate, and chat flows."
    },
    {
      "name": "macos_screen_capture",
      "command": "npm run verify:macos-screens",
      "cost": "zero_token",
      "proves": "LikemindedMac builds and every MacPrototypeScreen can be launched with seeded validation auth, backend state preloaded, and real app-window screenshots captured for mockup comparison."
    },
    {
      "name": "smoke-mvp",
      "command": "npm run smoke:mvp",
      "cost": "zero_token",
      "proves": "LikemindedMac app connects to backend API for auth, profile, circles, communities, meetings, soulmate, messages, and notifications; renders real data instead of static prototypes.",
      "description": "End-to-end API smoke test covers all critical flows"
    },
    {
      "name": "local_simulator_validation",
      "command": "npm run verify:simulator-local",
      "cost": "zero_token",
      "proves": "XcodeGen project builds, installs, launches fresh auth gate, proves empty-dev-db Profile onboarding, then enters the DEBUG signed-in Meet/Circles/Communities/Profile app through backend dev auth."
    }
  ],
  "simulator_validation": {
    "requires_model_backed_judgment": true,
    "preferred_agent": {
      "name": "validation-release",
      "model": "gpt-5.4-mini",
      "effort": "medium",
      "use_when": "Simulator or build verification is read-heavy, screenshot-based, or likely to produce long logs."
    },
    "checks": [
      "Fresh install shows Sign in with Apple gate, not old prototype tabs.",
      "Empty dev DB sign-in can route to Profile onboarding with no pre-seeded user data.",
      "After DEBUG placement sign-in, tabs are Meet, Circles, Communities, Profile.",
      "Meet can load RSVP state and scheduled meetup cards through the authenticated backend path.",
      "LiveKit join path requests a participant token only for scheduled meeting participants.",
      "Circles accept/swap/defer visibly round-trips through backend state.",
      "Profile edit and feedback paths are visible and retryable."
    ]
  },
  "subagents": {
    "default_policy": "Do not create new agents unless repeated work is not already owned by an existing project agent.",
    "allowed": [
      {
        "name": "validation-release",
        "model": "gpt-5.4-mini",
        "effort": "medium",
        "can_do": [
          "run deterministic graders",
          "inspect simulator screenshots",
          "summarize failing assertions and likely owner files",
          "propose the smallest fix"
        ],
        "cannot_do": [
          "change product scope",
          "weaken assertions",
          "touch secrets or external accounts",
          "change API/auth/persistence/deployment strategy",
          "edit files unless the main thread assigns a bounded patch"
        ]
      },
      {
        "name": "native-app",
        "model": "gpt-5.5",
        "effort": "medium",
        "can_do": [
          "bounded SwiftUI/native project fixes after deterministic failure evidence"
        ],
        "cannot_do": [
          "change backend contracts",
          "change TestFlight criteria",
          "replace the native project strategy without approval"
        ]
      },
      {
        "name": "backend-authority",
        "model": "gpt-5.5",
        "effort": "high",
        "can_do": [
          "bounded backend auth, persistence, or API contract fixes after deterministic failure evidence"
        ],
        "cannot_do": [
          "change iOS UI",
          "change external deployment provider",
          "loosen privacy or auth boundaries"
        ]
      }
    ]
  },
  "rubric": {
    "criteria": [
      {
        "name": "mvp_loop_correctness",
        "weight": 0.4,
        "min_score": 0.8
      },
      {
        "name": "deterministic_coverage",
        "weight": 0.25,
        "min_score": 0.8
      },
      {
        "name": "simulator_or_device_evidence",
        "weight": 0.2,
        "min_score": 0.7
      },
      {
        "name": "stale_surface_removed",
        "weight": 0.15,
        "min_score": 0.7
      }
    ],
    "min_score": 0.8
  },
  "completion_commit": {
    "required": true,
    "when": "After all required deterministic and local simulator validation passes, and before marking the session goal complete.",
    "must_include": [
      "goal.json",
      "all implementation, validation, doc, and stale-cleanup files changed for the session"
    ],
    "commit_message_template": "Validate <short session goal>"
  },
  "done_criteria": [
    "All 24 macOS static stub controls in validation/macos/*.json are pass with evidence (not pending).",
    "POST /v1/me/soulmate/preferences persists discovery, age range, and visibility.",
    "npm run verify:ledger-progress passes for macOS unimplemented-controls track owners.",
    "npm run smoke:mvp and LikemindedMac build pass.",
    "Validated session changes committed with goal.json before marking completed."
  ],
  "max_iterations": 10
}
